import React from 'react';
import ReactDOM from 'react-dom/client';
import Portifolio from './Files';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
      <div>
        <Portifolio />
      </div>
  </React.StrictMode>
);
